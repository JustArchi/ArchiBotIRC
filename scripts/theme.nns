; ––––––––––––––––––––––––––––––––––––––––
; NoNameScript v4.22 - dynamic theme file - coded by greeny & mute
; Don't edit anything in here unless you REALLY know what you're doing!
; File dynamically generated by NNScript 4.22 theme engine 1.03 at 2012-02-13, 00:01:22 in 0.125 second(s).
; ––––––––––––––––––––––––––––––––––––––––
alias msg {
if (!$isid) && ($remove($2-,$chr(32)) != $null) {
!.msg $$1-
if ($show) {
if ($1 ischan) || ($query($1)) || (=* iswm $1) {
if ($1 ischan) {
echo -ti12c own $1 11 $+ $align($cmod($1,$me),1) $+  $+ $align($me,15) 14• $2-
}
else {
echo -ti12c own $1  $align($me,15) 14• $2-
}
}
else {
echo $actcid(ti12c) normal 11 to 15 $align($1,11) 14• $2-
}
}
}
}
alias amsg {
var %t = $$1-
if (!$isid) && ($remove(%t,$chr(32)) != $null) {
!.amsg %t
if ($show) {
if ($chan(0)) {
var %c = $v1
while (%c) {
tokenize 32 $chan(%c) %t
echo -ti12c own $1 11 $+ $align($cmod($1,$me),1) $+  $+ $align($me,15) 14• $2-
dec %c
}
}
}
}
}
alias describe {
if (!$isid) {
!.describe $1-
if (!$show) { return }
if ($1 ischan) || ($query($1)) || (=* iswm $1) {
if ($1 ischan) {
echo -ti12c action $1 11 $+ $align($cmod($1,$me),1) $+  $+ $align($me,15) 14› $2-
}
else {
echo -ti12c action $1  $align($me,15) 14› $2-
}
}
else {
echo $actcid(ti12c) action 11 to 15 $align($1,11) 14› $2-
}
}
}
alias ame {
var %t = $$1-
if (!$isid) && ($remove(%t,$chr(32)) != $null) {
!.ame %t
if ($show) {
if ($chan(0)) {
var %c = $v1
while (%c) {
tokenize 32 $chan(%c) %t
echo -ti12c action $1 11 $+ $align($cmod($1,$me),1) $+  $+ $align($me,15) 14› $2-
dec %c
}
}
}
}
}
alias notice {
if (!$isid) {
.!notice $1-
if (!$show) { return }
echo $actcid(ti12c) notice 11 to 15 $align($1,11) 14- $2-
}
}
alias dns {
if (!$isid) {
if ($1) {
!.dns $1-
if ($show) {
var %h = $1,%f
if ($istok(-h -c,$1,32)) { var %f = $1,%h = $2 }
if (%f != -h) && (. !isin %h) && ($address(%h,5)) { %h = $gettok($v1,2,64) }
echo -ati12c info                  11• Resolving $thmbr(%h) $+ ...
}
}
}
}
alias ctcp {
if (!$isid) {
!.ctcp $1-
if ($2 == PING) { set -u100 $+(%,pingticks.,$cid,.,$1) $nnticks }
if ($show) && ($2 != DCC) {
echo $actcid(ti12c) ctcp 11 to 15 $align($1,11) 14? $thmbr($2) $3-
}
}
}
alias ctcpreply {
if (!$isid) {
!.ctcpreply $1-
if ($show) {
echo $actcid(ti12c) ctcp 11 to 15 $align($1,11) 14! $thmbr($2) $3-
}
}
}
on ^&*:notify:{
haltdef
echo $actcid(ti12c) info                  11• User15 $nick is now 09online
}
on ^&*:unotify:{
haltdef
echo $actcid(ti12c) info                  11• User15 $nick is now 04offline
}
alias thmecho {
if ($1 == -s) { var %e = -sti12c info }
elseif ($1 == -a) { var %e = $actcid(ti12c info) }
else { var %e = -ti12c info $1 }
echo %e                  11• $2-
}
alias thmerror {
if ($1 == -s) { var %e = -sti12c info }
elseif ($1 == -a) { var %e = $actcid(ti12c info) }
else { var %e = -ti12c info $1 }
echo %e                  11• 04Error: $2-
}
alias thmls {
if ($1 == -s) { var %e = -sti12c info }
elseif ($1 == -a) {
if (@* iswm $active) || ($cid != $activecid) { var %e = -sti12c info }
else { var %e = -ati12c info }
}
else { var %e = -ti12c info $1 }
echo %e                  11•
}
alias thmhl {
if ($1- != $null) {
return 15 $+ $1- $+ 
}
}
alias thmbr {
if ($1- != $null) {
return 15[ $+ $replace( $+ $1- $+ ,+,11+,-,11-,.,11.,!,11!,@,11@,~,11~) $+ 15]
}
}
on &^*:join:#:{
haltdef
playmircsound join
if ($chan !ischan) { return }
if ($nick != $me) {
var %e = $thmev($chan)
if (%e isnum 0-1) { %e = -ti12c join $chan }
elseif (%e == 2) { %e = -sti12c join }
elseif (%e == 3) { goto skip }
echo %e                  11• $nick $thmbr($address) has joined15 $chan
:skip
if (%qryevent) && ($query($nick)) && (j isin %qrytypes) {
echo -ti12c join $nick                  11• $+($nick,:,$chan) $thmbr($address) has joined15 $chan
}
}
else { set -u100 %joinprog. [ $+ [ $chan ] $+ ] . [ $+ [ $cid ] ] $nnticks }
}
on &!^*:part:#:{
haltdef
playmircsound part
if ($chan !ischan) { return }
var %e = $thmev($chan)
if (%e isnum 0-1) { %e = -ti12c part $chan }
elseif (%e == 2) { %e = -sti12c part }
elseif (%e == 3) { goto skip }
echo %e                  11•  $+ $nick $thmbr($address) has left15 $chan $thmbr($1-)
:skip
if (%qryevent) && ($query($nick)) && (p isin %qrytypes) {
echo -ti12c part $nick                  11•  $+ $+($nick,:,$chan) $thmbr($address) has left15 $chan $thmbr($1-)
}
}
on &^*:kick:#:{
haltdef
playmircsound kick
if ($chan !ischan) { return }
if ($knick == $me) {
echo -sti12c kick                  11• You were kicked from $thmbr($chan) by15 $nick
}
var %e = $thmev($chan)
if (%e isnum 0-1) { %e = -ti12c kick $chan }
elseif (%e == 2) { %e = -sti12c kick }
elseif (%e == 3) { goto skip }
echo %e                  11• $nick $thmbr($address) has kicked15 $knick $thmbr($1-)
if ($away.chklog($null).event) && ($ccontrol($chan)) {
echo -ti12c kick $specialcidwin(Awaylog)                  11• $+($nick,:,$chan) $thmbr($address) has kicked15 $knick $thmbr($1-)
}
:skip
if (%qryevent) && (k isin %qrytypes) {
if ($query($knick)) {
echo -ti12c kick $knick                  11• $+($nick,:,$chan) $thmbr($address) has kicked15 $knick $thmbr($1-)
}
if ($query($nick)) {
echo -ti12c kick $nick                  11• $+($nick,:,$chan) $thmbr($address) has kicked15 $knick $thmbr($1-)
}
}
}
on &^*:quit:{
haltdef
var %c = $comchan($nick,0)
while (%c) {
var %e = $thmev($comchan($nick,%c))
if (%e isnum 0-1) { %e = -ti12c quit $comchan($nick,%c) }
elseif (%e == 2) { %e = -sti12c quit }
elseif (%e == 3) {
%e = -sti12c quit
echo %e                  11• $nick $thmbr($address) has quit 15IRC
%e = -ti12c quit $comchan($nick,%c)
}
elseif (%e == 4) { goto skip }
echo %e                  11• $nick $thmbr($address) has quit 15IRC
:skip
dec %c
}
if (%qryevent) && ($query($nick)) && (q isin %qrytypes) {
echo -ti12c quit $nick                  11• $nick $thmbr($address) has quit 15IRC
}
}
on &^*:nick:{
haltdef
if ($nick === $newnick) { return }
var %c = $comchan($newnick,0),%q = $query(0)
while (%c) {
var %e = $thmev($comchan($newnick,%c))
if (%e isnum 0-1) { %e = -ti12c nick $comchan($newnick,%c) }
elseif (%e == 2) { goto skip }
echo %e                  11• $nick is now known as15 $newnick
:skip
dec %c
}
if ($nick == $me) {
echo -sti12c nick                  11• Your nick is now15 $newnick
}
if (%qryevent) {
if ($query($newnick)) && (n isin %qrytypes) {
echo -ti12c nick $newnick                  11• $nick is now known as15 $newnick
}
elseif ($nick == $me) && (o isin %qrytypes) {
while (%q) {
echo -ti12c nick $query(%q)                  11• $nick is now known as15 $newnick
dec %q
}
}
}
}
on &^*:topic:#:{
haltdef
if ($chan !ischan) { return }
var %e = $thmev($chan)
if (%e isnum 0-1) { %e = -ti12c topic $chan }
elseif (%e == 2) { %e = -sti12c topic }
elseif (%e == 3) { goto skip }
echo %e                  11• $nick changed the 15topic to: $1-
if ($away.chklog($null).event) && ($ccontrol($chan)) {
echo -ti12c topic $specialcidwin(Awaylog)                  11• $+($nick,:,$chan) changed the 15topic to: $1-
}
:skip
}
ctcp &*:*:*:{
if (!$istok(dcc version,$1,32)) {
haltdef
if ($1 == ping) { .ctcpreply $nick PING $ctime }
if ($chan) {
if ($chan !ischan) { return }
var %e = $thmev($chan)
if (%e isnum 0-1) { %e = -ti12c ctcp $chan }
elseif (%e == 2) { %e = -sti12c ctcp }
echo %e 11from14 #15 $+ $align($nick,10) 14? $thmbr($1) $2-
}
else {
var %e = $thm.active(ctcp,ti12c ctcp)
echo %e 11from15 $align($nick,11) 14? $thmbr($1) $2-
}
}
}
on &^*:rawmode:#:{
haltdef
if ($chan !ischan) { return }
var %e = $thmev($chan)
if (%e isnum 0-1) { %e = -ti12c mode $chan }
elseif (%e == 2) { %e = -sti12c mode }
elseif (%e == 3) { goto skip }
echo %e                  11• $nick set mode $thmbr($1-)
:skip
}
on &^*:invite:#:{
haltdef
playmircsound invite
var %e = $thm.active(invite,ti12c invite)
echo %e                  11• You have received an invitation to join15 $chan from15 $nick
}
on &^*:usermode:{
haltdef
echo -sti12c mode                  11• 15Your modes are now $thmbr($1-)
}
on &^*:text:*:#:{
haltdef
playmircsound channeltext
if ($chan !ischan) { return }
echo -lbfmti12c normal $chan 11 $+ $align($cmod($chan,$nick),1) $+  $+ $align($nick,15) 14• $strip($1-,mo)
if (%hlown) && ($isachl($1-)) {
chkhlwin
echo -mti12c normal $specialcidwin(Highlight) 11 $+ $align($cmod($chan,$nick),1) $+  $+ $align($+($nick,:,$chan),15) 14• $strip($1-,mo)
}
if ($away.chklog($strip($1-))) {
echo -mti12c normal $specialcidwin(Awaylog) 11 $+ $align($cmod($chan,$nick),1) $+  $+ $align($+($nick,:,$chan),15) 14• $strip($1-,mo)
}
}
on &^*:text:*:?:{
haltdef
playmircsound querytext
if ($query($nick)) {
echo -lbfmti12c normal $nick  $align($nick,15) 14• $strip($1-,mo)
}
elseif (%thm.singlemsg) {
echo -lbfmdti12c normal 11from15 $align($nick,11) 14• $1-
}
else {
echo $thm.active(query,lbfmti12c) normal 11from15 $align($nick,11) 14• $strip($1-,mo)
}
if (%hlown) && ($isachl($1-)) {
chkhlwin
echo -mti12c normal $specialcidwin(Highlight)  $align($nick,15) 14• $strip($1-,mo)
}
}
on &^*:chat:*:{
haltdef
playmircsound chattext
if (ACTION ?* iswm $1-) {
echo -lbfmti12c normal =$nick  $align($nick,15) 14› $strip($mid($1-,9,-1),mo)
if (%hlown) && ($isachl($1-)) {
chkhlwin
echo -mti12c normal $specialcidwin(Highlight)  $align($nick,15) 14› $strip($mid($1-,9,-1),mo)
}
}
else {
echo -lbfmti12c normal =$nick  $align($nick,15) 14• $strip($1-,mo)
if (%hlown) && ($isachl($1-)) {
chkhlwin
echo -mti12c normal $specialcidwin(Highlight)  $align($nick,15) 14• $strip($1-,mo)
}
}
}
on &^*:action:*:#:{
haltdef
if ($chan !ischan) { return }
echo -lbfmti12c action $chan 11 $+ $align($cmod($chan,$nick),1) $+  $+ $align($nick,15) 14› $strip($1-,mo)
if (%hlown) && ($isachl($1-)) {
chkhlwin
echo -mti12c action $specialcidwin(Highlight) 11 $+ $align($cmod($chan,$nick),1) $+  $+ $align($+($nick,:,$chan),15) 14› $strip($1-,mo)
}
if ($away.chklog($strip($1-))) {
echo -mti12c action $specialcidwin(Awaylog) 11 $+ $align($cmod($chan,$nick),1) $+  $+ $align($+($nick,:,$chan),15) 14› $strip($1-,mo)
}
}
on &^*:action:*:?:{
haltdef
if (%thm.singlemsg) {
echo -dti12c action 11from15 $align($nick,11) 14› $1-
}
elseif ($query($nick)) {
echo -lbfmti12c action $nick  $align($nick,15) 14› $strip($1-,mo)
}
else {
echo $thm.active(query,lbfmti12c) action 11from15 $align($nick,11) 14› $strip($1-,mo)
}
if (%hlown) && ($isachl($1-)) {
chkhlwin
echo -mti12c action $specialcidwin(Highlight)  $align($nick,15) 14› $strip($1-,mo)
}
}
on &^*:notice:*:#:{
haltdef
playmircsound notice
if ($chan !ischan) { return }
if (%notice.sepwin) {
notice.chkwin
var %e = -mti12c notice $specialcidwin(Notices)
}
else { var %e = -mti12c notice $chan }
echo %e 11from14 #15 $+ $align($nick,10) 14- $1-
}
on &^*:notice:*:?:{
haltdef
playmircsound notice
if (%notice.sepwin) {
notice.chkwin
var %e = -mti12c notice $specialcidwin(Notices)
}
else {
if ($qnetchkwlc($fulladdress,$1-)) { var %e = -mti12c notice $v1 }
else { var %e = $thm.active(notice,mti12c notice) }
}
echo %e 11from15 $align($nick,11) 14- $1-
}
on &^*:snotice:*:{
haltdef
playmircsound notice
if (%notice.sepwin) {
notice.chkwin
var %e = -mti12c notice $specialcidwin(Notices)
}
else { var %e = -msti12c notice }
echo %e 11from15 $align($nick,11) 14- $1-
}
on &*:dns:{
haltdef
if ($dns(0)) {
var %i = 1,%t = $dns(0)
while (%i <= %t) {
echo -ati12c info                  11• Resolved $thmbr($dns(%i)) to $thmbr($remtok($dns(%i).ip $dns(%i).addr,$dns(%i),1,32))
inc %i
}
}
else {
echo -ati12c info                  11• Unable to resolve $thmbr($iif($address,$v1,$nick))
}
}
on &*:ctcpreply:*:{
haltdef
if ($1 == PING) {
if ($($+(%,pingticks.,$cid,.,$nick),2)) {
var %tmp = $v1,%rpl = $round($calc($nnticks - %tmp),3)
if (%rpl < 1) { %rpl = $calc(%rpl *1000) ms }
else { %rpl = %rpl second(s) }
unset $+(%,pingticks.,$cid,.,$nick)
}
else { %rpl = $duration($calc($ctime - $2)) }
tokenize 32 PING %rpl
}
var %e = $thm.active(ctcp,ti12c ctcp)
echo %e 11from15 $align($nick,11) 14! $thmbr($1) $2-
}
on ^&*:wallops:*:{
haltdef
if ($1 == *) { var %w = $specialcidwin(Wallusers),%t = $2- }
elseif ($1 == $) { var %w = $specialcidwin(Wallops),%t = $2- }
else { var %w = $specialcidwin(Wallops),%t = $1- }
if (!$window(%w)) {
window -nk0g1e %w
thmecho %w Wall message window for $thmhl($curconserv) opened on $thmhl($longtime) $+ .
echo %w 
}
echo -lmti12c wallops %w  $align($nick,15) 14~ $strip(%t,mo)
}
raw 324:*:{
if ($halted) { return }
haltdef
echo -ti12c info $2                  11• Channel modes on15 $2 are $thmbr($3-)
}
raw 329:*:{
if ($halted) { return }
haltdef
echo -ti12c info $2                  11• Channel15 $2 was created on $thmbr($longtime($3))
}
raw 367:*:{
if ($halted) { return }
haltdef
echo -ti12c info $2                  11• $2 ban $thmbr($3) set by15 $4 $+  $thmbr($longtime($5))
}
raw 368:*:{
if ($halted) { return }
haltdef
echo -ti12c info $2                  11• End of15 $2 ban list
}
raw 404:*:{
if ($halted) { return }
haltdef
echo -ti12c info $2                  11• Unable to message15 $2
}
raw 482:*:{
if ($halted) { return }
haltdef
echo -ti12c info $2                  11• You're not opped in15 $2
}
raw 396:*:{
if ($halted) { return }
haltdef
echo -sti12c info                  11• Your host is now hidden $thmbr($2)
}
raw 451:*:{
if ($halted) { return }
haltdef
echo -sti12c info                  11• You have to register first!
}
raw 311:*:{
if ($halted) { return }
haltdef
if (%whois. [ $+ [ $cid ] ]) {
echo $thm.active(whois,ti12c) whois                  11•
}
else {
set %whois. $+ $cid 1
}
echo $thm.active(whois,ti12c) whois                  11•
echo $thm.active(whois,ti12c) whois            Whois 11•15 $2 $thmbr($6-)
echo $thm.active(whois,ti12c) whois          Address 11•15 $+($3,@,$4)
}
raw 319:*:{
if ($halted) { return }
haltdef
echo $thm.active(whois,ti12c) whois         Channels 11•15 $replace( $+ $sorttok($3-,32,c) $+ ,$chr(35),11#15,+,14+15,%,14%15,@,14@15)
}
raw 312:*:{
if ($halted) { return }
haltdef
if (%whois. [ $+ [ $cid ] ]) {
echo $thm.active(whois,ti12c) whois           Server 11•15 $3 $thmbr($4-)
}
else {
echo $thm.active(whois,ti12c) whois           Server 11•15 $3 $thmbr($4-)
}
}
raw 313:*:{
if ($halted) { return }
haltdef
echo $thm.active(whois,ti12c) whois           Status 11•15 $5-
}
raw 314:*:{
if ($halted) { return }
haltdef
set %whowas. $+ $cid 1
echo $thm.active(whois,ti12c) whois                  11•
echo $thm.active(whois,ti12c) whois           Whowas 11•15 $2 $thmbr($6-)
echo $thm.active(whois,ti12c) whois          Address 11•15 $+($3,@,$4)
}
raw 301:*:{
if ($halted) { return }
haltdef
if (%whois. [ $+ [ $cid ] ]) {
echo $thm.active(whois,ti12c) whois             Away 11•15 $3-
}
elseif (%whowas. [ $+ [ $cid ] ]) {
echo $thm.active(whois,ti12c) whois             Away 11•15 $3-
}
elseif ($2 != $me) && (!%away.seen. [ $+ [ $2 ] $+ ] . [ $+ [ $cid ] ]) {
set -u300 %away.seen. [ $+ [ $2 ] $+ ] . [ $+ [ $cid ] ] 1
echo $thm.active(away,ti12c) info                  11• $2 is away $thmbr($3-)
}
}
raw 369:*:{
if ($halted) { return }
haltdef
if (%whowas. [ $+ [ $cid ] ]) {
echo $thm.active(whois,ti12c) whois                  11•
unset %whowas. $+ $cid
}
}
raw 330:*:{
if ($halted) { return }
haltdef
echo $thm.active(whois,ti12c) whois             Auth 11•15 $3
}
raw 338:*:{
if ($halted) { return }
haltdef
echo $thm.active(whois,ti12c) whois     Real address 11•15 $3 $thmbr($4-)
}
raw 317:*:{
if ($halted) { return }
haltdef
echo $thm.active(whois,ti12c) whois             Idle 11•15 $duration($3)
echo $thm.active(whois,ti12c) whois          Sign-on 11•15 $asctime($4) $thmbr($duration($calc($ctime - $4)))
}
raw 318:*:{
if ($halted) { return }
haltdef
if (%whois. [ $+ [ $cid ] ]) {
echo $thm.active(whois,ti12c) whois                  11•
unset %whois. $+ $cid
}
}
raw 331:*:{
if ($halted) { return }
haltdef
if ($me ison $2) { var %e = -ti12c topic $2 }
else { var %e = $actcid(ti12c) topic }
echo %e                  11• No topic in15 $2 set
}
raw 332:*:{
if ($halted) { return }
haltdef
if (%joinprog. [ $+ [ $2 ] $+ ] . [ $+ [ $cid ] ]) { return }
if ($me ison $2) { var %e = -ti12c topic $2 }
else { var %e = $actcid(ti12c) topic }
echo %e                  11• Topic: $3-
}
raw 333:*:{
if ($halted) { return }
haltdef
if (%joinprog. [ $+ [ $2 ] $+ ] . [ $+ [ $cid ] ]) {
set -u100 $+(%,topicset.,$2,.,$cid) $3-
return
}
if ($me ison $2) { var %e = -ti12c topic $2 }
else { var %e = $actcid(ti12c) topic }
echo %e                  11• Set by15 $3 on15 $thmbr($asctime($4))
}
raw 341:*:{
if ($halted) { return }
haltdef
echo $actcid(ti12c) invite                  11• You have sent15 $2 an invitation to join15 $3
}
raw 443:*:{
if ($halted) { return }
haltdef
echo $actcid(ti12c) invite                  11• User15 $2 is already on15 $3
}
raw 353:*:{
if ($halted) { return }
haltdef
if (!%thm.hnames) || (%names.show. [ $+ [ $cid ] ]) {
echo -sti12c info                  11• Users on15 $3 $+ : $4-
}
}
raw 355:*:{
if ($halted) { return }
haltdef
if (!%thm.hnames) || (%names.show. [ $+ [ $cid ] ]) {
echo -sti12c info                  11• Users on15 $3 $+ : $4-
}
}
raw 366:*:{
if ($halted) { return }
haltdef
if (!%thm.hnames) || (%names.show. [ $+ [ $cid ] ]) {
echo -sti12c info                  11• End of15 $2 user list
}
if (%joinprog. [ $+ [ $2 ] $+ ] . [ $+ [ $cid ] ]) {
var %nt = $v1,%s = $iif($round($calc($nnticks - %nt),2) isnum 0-1000,$v1,<unknown>),%g = %topicset. [ $+ [ $2 ] $+ ] . [ $+ [ $cid ] ],%n = $iif($wd(%g,1,32),$v1,<unknown>),%d = $iif($wd(%g,2,32),$longtime($v1),<unknown>),%a = $iif($wd(%g,2,32),$duration($calc($ctime - $v1)),<unknown>)
if ($me ison $2) {
haltdef
echo -ti12c join $2                  11• Joined15 $2
if ($thmif($chan($2).topic)) { echo -ti12c join $2                  11• Topic: $chan($2).topic }
if ($thmif($chan($2).topic)) { echo -ti12c join $2                  11• Set by15 %n on15 $thmbr(%d) }
}
}
unset %names.show. $+ $cid %joinprog. $+ $2 $+ . $+ $cid %topicset. $+ $2 $+ . $+ $cid
}
raw 421:*:{
if ($halted) { return }
haltdef
echo $actcid(ti12c) info                  11• 15 $+ $2 is an unknown command
}
raw 352:*:{
if ($halted) { return }
haltdef
echo $actcid(ti12c) whois                  11• User15 $6 $thmbr($+($3,@,$4)) is on15 $2 $+ . His real name is15 $9- $+ , his status15 $7
}
raw 315:*:{
if ($halted) { return }
haltdef
echo $actcid(ti12c) whois                  11• End of /WHO list for15 $2
}
raw 433:*:{
if ($halted) { return }
haltdef
echo $actcid(ti12c) info                  11• The nickname15 $2 is already in use.
}
raw 471:*:{
if ($halted) { return }
haltdef
echo $actcid(ti12c) info                  11• Can't join15 $2 $+ : channel is full
}
raw 473:*:{
if ($halted) { return }
haltdef
echo $actcid(ti12c) info                  11• Can't join15 $2 $+ : invite only
}
raw 474:*:{
if ($halted) { return }
haltdef
echo $actcid(ti12c) info                  11• Can't join15 $2 $+ : banned
}
raw 475:*:{
if ($halted) { return }
haltdef
echo $actcid(ti12c) info                  11• Can't join15 $2 $+ : incorrect key
}
raw 477:*:{
if ($halted) { return }
if ($left($2,1) isin $chantypes) {
haltdef
echo $actcid(ti12c) info                  11• Can't join15 $2 $+ : registered only
}
}
raw 401:*:{
if ($halted) { return }
haltdef
echo $actcid(ti12c) info                  11• The user15 $2 doesn't exist
unset $+(%,whois.,$2,.,$cid)
}
raw 406:*:{
if ($halted) { return }
haltdef
echo $actcid(ti12c) info                  11• The user15 $2 doesn't exist
}
raw 403:*:{
if ($halted) { return }
haltdef
echo $actcid(ti12c) info                  11• The channel15 $2 doesn't exist
}
raw 251:*:{
if ($halted) { return }
haltdef
echo -sti12c info                  11• There are15 $4 users and15 $7 invisible on15 $10 servers
}
raw 252:*:{
if ($halted) { return }
haltdef
echo -sti12c info                  11• There are15 $2 operator(s) online
}
raw 253:*:{
if ($halted) { return }
haltdef
echo -sti12c info                  11• There are15 $2 unknown connection(s)
}
raw 254:*:{
if ($halted) { return }
haltdef
echo -sti12c info                  11• 15 $+ $2 channels formed
}
raw 255:*:{
if ($halted) { return }
haltdef
echo -sti12c info                  11• There are15 $4 clients and15 $7 servers
}
raw 001:*:{
if ($halted) { return }
haltdef
echo -sti12c info                  11• Welcome to the Internet Relay Network,15 $8
}
raw 002:*:{
if ($halted) { return }
haltdef
if (!%thm.hwmsg) {
echo -sti12c info                  11• Your host is15 $left($5,-1) $+ , running version15 $8
}
}
raw 003:*:{
if ($halted) { return }
haltdef
if (!%thm.hwmsg) {
echo -sti12c info                  11• This server was created15 $6-
}
}
raw 004:*:{
if ($halted) { return }
haltdef
if (!%thm.hwmsg) {
echo -sti12c info                  11• Server:15 $2 $+ , version:15 $3 $+ , user modes:15 $4 $+ , channel modes:15 $5
}
}
raw 005:*:{
if ($halted) { return }
haltdef
if (!%thm.hwmsg) {
echo -sti12c info                  11• Protocols supported by this server are15 $deltok($2-,-5-,32)
}
}
raw *:*:{
if ($halted) { return }
if (!$istok(321 322 323,$numeric,32)) {
haltdef
echo $whois.echo $+ ti12c info                  11• $2-
}
}
; ––––––––––––––––––––––––––––––––––––––––
; End of file
; ––––––––––––––––––––––––––––––––––––––––
